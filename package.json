{
	"name": "squirrel",
	"private": true,
	"scripts": {
		"lint": "cd backend && pnpm run lint && cd ../frontend && pnpm run lint",
		"bot": "cd backend && pnpm run bot",
		"http": "cd backend && pnpm run http",
		"migrate": "cd backend && pnpm run migrate --",
		"test": "cd backend && pnpm run test"
	}
}
