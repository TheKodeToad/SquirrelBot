{
	"name": "squirrel",
	"private": true,
	"scripts": {
		"bot": "cd backend && pnpm run bot",
		"http": "cd backend && pnpm run http",
		"migration": "cd backend && pnpm run migration",
		"typecheck": "pnpm --stream run -r typecheck",
		"lint": "pnpm --stream run -r lint",
		"reformat": "pnpm --stream run -r reformat",
		"formatting": "pnpm --stream run -r formatting",
		"test": "pnpm --stream run -r test"
	},
	"devDependencies": {
		"prettier": "3.6.2"
	}
}
