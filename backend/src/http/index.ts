import { type SquirrelContext } from "#index.ts";

// currently this just aliases context
// this alias exists to be able to add more stuff later
export interface SquirrelHTTPContext extends SquirrelContext {}
